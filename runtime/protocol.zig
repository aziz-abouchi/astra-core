pub const Protocol = struct {
    pub fn check(actor_id: []const u8, msg: []u8) bool {
        // Implémente la projection MPST et vérifie que le message est autorisé
        return true; // Placeholder, à remplacer par la projection réelle
    }
};
