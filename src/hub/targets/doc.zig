// src/hub/targets/doc.zig
pub fn emitDocumentation(node: *ast.Node, linker: *Linker) !void {
    // Génère un tableau Markdown/HTML avec :
    // - L'intention (Heaven)
    // - La contrainte physique (Unify)
    // - L'implémentation réelle (Linker)
}
